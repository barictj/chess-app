import { View, Text } from "react-native";

export default function ChessScreen() {
    return (
        <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
            <Text>Chess screen (working)</Text>
        </View>
    );
}
